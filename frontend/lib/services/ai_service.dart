/// Service handling conversational inputs for the AI Health/Voice Assistant.
/// NOTE: This communicates with the FastAPI Gateway, NOT directly with the AI model.
class AIService {
  // TODO: Connect to FastAPI AI proxy endpoints
  // Example: Future<String> sendMessage(String message) async {}
}
