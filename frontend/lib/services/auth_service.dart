/// Service responsible for communicating with the backend authentication APIs.
class AuthService {
  // TODO: Implement login, registration, and token management via FastAPI
  // Example: Future<UserModel> login(String email, String password) async {}
}
